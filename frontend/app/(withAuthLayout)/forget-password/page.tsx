import ForgetPasswords from "@/components/auth-section/ForgetPasswords";

export const metadata = {
    title: "Forgot Password - My Website",
    description: "Reset your password securely on My Website.",
  };

  
const ForgetPassword = () => {
  return (
    <div>
      <ForgetPasswords/>
    </div>
  )
}

export default ForgetPassword